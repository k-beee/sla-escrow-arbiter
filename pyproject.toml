[project]
name = "sla-escrow-arbiter"
version = "0.1.0"
description = "Milestone & SLA Escrow Arbiter on GenLayer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
